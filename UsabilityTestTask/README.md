# Feature Task: Implement Safe Division

You are assigned to implement a safe division function.

Implement the function `safe_divide(a, b)` in calculator.py.

Requirements:

1. If b == 0 → return the string "Error"
2. Otherwise return the result of a / b
3. The result should be rounded to 2 decimal places
4. Always return a float (except when returning "Error")

Do NOT modify test_calculator.py.
